#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../locally/bin/locally" "$@"
  ret=$?
else 
  node  "$basedir/../locally/bin/locally" "$@"
  ret=$?
fi
exit $ret
